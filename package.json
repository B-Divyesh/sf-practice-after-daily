{
  "name": "practice-after-daily",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && cp staticwebapp.config.json dist/staticwebapp.config.json",
    "preview": "vite preview",
    "test": "node scripts/test.mjs",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.11.0",
    "@playwright/test": "1.58.2",
    "typescript": "~5.9.3",
    "vite": "~7.1.7"
  }
}
